import 'package:flutter/material.dart';

  class Khataservices extends StatelessWidget {
  const Khataservices({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(title: Text('Khataservices'),),

    );
  }
}
