import 'package:flutter/material.dart';
  class TrafficFine extends StatelessWidget {
  const TrafficFine({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(title: Text('TrafficFine'),),

    );
  }
}
