import 'package:flutter/material.dart';
class RcExtraction extends StatelessWidget {
  const RcExtraction({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(title: Text('Rc Extraction'),),

    );
  }
}
