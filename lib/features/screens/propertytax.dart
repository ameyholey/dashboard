import 'package:flutter/material.dart';
  class Propertytax extends StatelessWidget {
  const Propertytax({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(title: Text('Propertytax'),),

    );
  }
}
